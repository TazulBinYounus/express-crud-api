<%- include("../partials/_header") %>

<div class="container mt-5">
    <div class="row mb-4">
        <div class="col-md-10">
            <h4 class="text-white ">Invoice List</h4>
        </div>

        <div class=" col-md-2 ">
            <a href="/create" class="btn btn-info">Add New Invoce</a>
        </div>
    </div>
  <div class="row">
    <div class="col-md-12">
      <table class="table table-dark table-bordered table-hover">
        <thead>
          <tr>
            <td>n°</td>
            <td>InvoiceID</td>
            <td>Name</td>
            <td>Description</td>
            <td>Date</td>
            <td>Actions</td>
          </tr>
        </thead>
        <tbody>
          <% if (invoice) { %> <% for(var i = 0; i < invoice.length; i++) {
          %>
          <tr>
            <td><%= (i + 1) %></td>
            <td><%= invoice[i].invoice_id %></td>
            <td><%= invoice[i].name %></td>
            <td><%= invoice[i].description %></td>
            <td><%= invoice[i].created_at %></td>
            <td class="gap-2">
              <a href="/update/<%= invoice[i].id %>" class="text-white">
                Edit
              </a>
              <a href="/delete/<%= invoice[i].id %>" class="text-white">
                Delete
              </a>
            </td>
          </tr>
          <% } %> <% } %>   
        </tbody>
      </table>
    </div>
    <!-- <div class="col-md-4">
      <div class="card-body">
        <form action="/add" method="POST">
          <input
            type="text"
            name="name"
            placeholder="Name"
            class="form-control mb-2"
            autofocus
          />
          <input
            type="email"
            name="address"
            placeholder="Address"
            class="form-control mb-2"
          />
          <input
            type="text"
            name="phone"
            placeholder="Phone"
            class="form-control mb-2"
          />

          <button type="submit" class="btn btn-info">save customer</button>
        </form>
      </div>
    </div> -->
  </div>
</div>

<%- include("../partials/_footer") %>
